import { Environment } from "./interfaces/environments";

export const environment: Environment = {
    api: {
        service_endpoint: 'https://localhost:7079',
        hub_endpoint: 'https://localhost:7079'
    }
}